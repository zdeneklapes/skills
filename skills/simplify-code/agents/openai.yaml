interface:
  display_name: "Simplify Code"
  short_description: "Refine changed code without behavior drift"
  default_prompt: "Use $simplify-code to simplify the recently changed code while preserving behavior."
